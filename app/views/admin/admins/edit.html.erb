<%= form_for @admin, url: admin_admin_path(@admin) do |f| %>
	<h1> Edit admin </h1>
	<p>
		<%= f.label :email %>
		<%= f.text_field :email %>
	</p>
	<%= f.submit 'Update' %>
<% end %>
<%= link_to 'Show admin', admin_admin_path(current_admin) %>